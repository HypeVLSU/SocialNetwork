Данное ПО работает по социальной сети "Вконтакте"
Далее разберем каждый компонент ПО:
1. graphList - компонент, который позволяет анализировать данные. На вход он принимает список пользователей (их идентификаторы). Затем строит граф связей между этими пользователями.
2. informationConsole - компонент, который занимается сбором информации.На вход он принимает список пользователей (их идентификаторы). Затем собирает информацию о каждом конкретном пользователе и записывает её в текстовый файл (папка Information)
3. newGraph - компонент, который позволяет анализировать данные. На вход он принимает пользователя (его идентификатор). Затем строит граф связей этого пользователя с его соц. друзьями, и его друзей с их друзьями.
4. test - компонент для тестов
